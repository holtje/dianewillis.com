language: ruby
env: DB=postgres
rvm: 2.1.5
script: "bundle exec rake travis"
before_script:
  - "cp config/database.travisci.yml config/database.yml"
  - "psql -c 'create database dianewillis_test;' -U postgres >/dev/null"
